#include "main.h"
/**
** _memset - put 1 when is a uppercase
** @s: takes an integer
** @b: takes an integer
** @n: takes an integer
** Return: 1 if is uppercase and 0 if is lowcase
**/
char *_memset(char *s, char b, unsigned int n)
{
return (0);
}
