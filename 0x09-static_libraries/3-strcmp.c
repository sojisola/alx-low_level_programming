#include "main.h"
/**
** _strcmp - put 1 when is a uppercase
** @s1: takes in an integer
** @s2: takes in an integer
** Return: 1 if is uppercase and 0 if is lowcase
**/
int _strcmp(char *s1, char *s2)
{
return (0);
}
